import {
    Box,
    FormControl,
    TextField,
    Button,
    FormLabel,
    TextareaAutosize,
    Grid,
} from '@mui/material';
import * as React from 'react';

export default function QuestionVisualizations(props){
    return (
        <Box>
            <h3>
                Visualizations
            </h3>
        </Box>
    );
}