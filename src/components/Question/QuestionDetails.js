import {
    Box,
    FormControl,
    TextField 
} from '@mui/material';
import * as React from 'react';

export default function QuestionDetails(){
    return (
        <Box>
            Quiz Details
        </Box>
    );
}