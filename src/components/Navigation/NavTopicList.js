import {
    Box, 
    /*List,
    ListItem,
    ListItemButton,
    ListItemText,
    Typography,*/
} from '@mui/material';
import * as React from 'react';

export default function NavTopicList(props){
    return (
        <Box>
            List
        </Box>
    );
}