import {
    Box,
    FormControl,
    TextField 
} from '@mui/material';
import * as React from 'react';

export default function QuizPreview(){
    return (
        <Box>
            Quiz Preview
        </Box>
    );
}